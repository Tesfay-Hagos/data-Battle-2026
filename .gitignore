# ── Data (lives on Google Drive, never commit) ────────────────────────────
data/
*.csv
*.parquet
*.pkl
*.h5

# ── Generated outputs (Drive is source of truth) ─────────────────────────
outputs/

# ── Notebooks (exploratory only — source code lives in src/) ─────────────
notebooks/

# ── LaTeX build artefacts (keep .tex and .pdf, discard build leftovers) ──
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.nav
report/*.snm
report/*.synctex.gz
report/*.fls
report/*.fdb_latexmk

# ── Python ────────────────────────────────────────────────────────────────
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# ── Misc ──────────────────────────────────────────────────────────────────
=1.28
report.zip
.DS_Store
.idea/
.vscode/
*.swp
